Программа выполняет поиск номера элемента массива, после которого идут все повторяющиеся.

Для работы программы необходимы библиотеки:
- libext.so - библиотека с внешней функцией (собрать можно из исходного файла в папке ext). Необходимо для лаборатрной работы.
- libextfunc.so - библиотека с разными функциями для упрощения работы с C++ (библиотеку можно собрать из исходников, лежащих в соседнем репозитории extFunc)
- libdl.so - стандартная библиотека для обеспечения поздней линковки
- libpopt.so - стандартная библиотека для разбора параметров командной строки

В частности вторая библиотека нужна для функции split.

После сборки программы её надо запустить. Формат запуска:
./Lab3 [--debug] [-i file]

Флаг --debug говорит о том, что нужно выводить на экран всю отладочную информацию.
Флаг -i file, где file - имя файла, говорит, что массив нужно брать из файла. Формат файла:
x1 x2 x3 x4 x5 x6
где xN - числа
